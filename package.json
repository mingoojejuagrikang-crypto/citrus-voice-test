{
  "name": "citrus-survey-v006-codex-pwa-voice",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:version": "vite build && rm -rf versions/v006 && mkdir -p versions/v006 && cp -R dist/. versions/v006/",
    "build:codex": "vite build && rm -rf codex && mkdir -p codex && cp -R dist/. codex/"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.0.1",
    "vite": "^8.0.8"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
